name: Notify on Push

# Déclenche le workflow à chaque push sur main
on:
  push:
    branches:
      - main

jobs:
  send_email:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Send Email Notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.farah.derbel2016@gmail.com }}   # ton email
          password: ${{ secrets.samar98654042 }}   # mot de passe d'application Gmail
          subject: "GitHub Push Notification"
          body: "Un push a été effectué sur la branche main de HousePricePrediction."
          to: "ahmed.tounsi@esprit.tn"
